# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase
- Feature 29 complete.

## Current Goal
- Awaiting next feature spec.

## Completed

- Feature 01: Design System — shadcn/ui configured, 7 UI components added (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea), lucide-react installed, globals.css dark theme tokens set up with @theme inline Tailwind v4 mappings, lib/utils.ts cn() helper created.
- Feature 02: Editor Shell — EditorNavbar (fixed top bar, sidebar toggle with PanelLeftOpen/PanelLeftClose, left/center/right layout) and ProjectSidebar (floating overlay, slide-in from left, Projects title + X close, My Projects/Shared tabs with empty states, New Project button) created in components/editor/.
- Feature 03: Auth — @clerk/ui installed; Clerk env vars added (SIGN_IN_URL, SIGN_UP_URL, AFTER_SIGN_IN_URL, AFTER_SIGN_UP_URL); proxy.ts at project root wraps clerkMiddleware with createRouteMatcher to protect all routes except /sign-in and /sign-up; ClerkProvider wraps root layout using dark theme from @clerk/ui/themes with CSS variable overrides (no hardcoded colors); sign-in and sign-up pages use two-panel layout (info panel left on lg+, Clerk form right); app/page.tsx redirects authenticated users to /editor and unauthenticated users to /sign-in; UserButton added to EditorNavbar right section; minimal app/editor/page.tsx shell created with EditorNavbar and ProjectSidebar.
- Feature 04: Project Dialogues — /editor home screen with heading, description, and New Project button; useProjectDialogs hook managing dialog/form/loading state; Create/Rename/Delete dialogs in components/editor/project-dialogs.tsx; sidebar updated with project item list, hover-reveal rename/delete actions (owned projects only), mobile backdrop scrim, New Project button wired to Create dialog; mock project data in lib/mock-projects.ts; no API calls or persistence.
- Feature 05: Prisma — Project and ProjectCollaborator models in prisma/models/project.prisma (ProjectStatus enum DRAFT/ARCHIVED, cascade delete, unique project/email constraint, indexes on ownerId, createdAt, email, projectId/createdAt); lib/prisma.ts cached singleton branching on DATABASE_URL (prisma+postgres:// → accelerateUrl, postgres:// → @prisma/adapter-pg); migration 20260616200841_init applied; client generated to app/generated/prisma/.
- Feature 06: Project APIs — REST endpoints in app/api/projects/route.ts (GET lists owner's projects ordered by createdAt desc; POST creates project with default name "Untitled Project") and app/api/projects/[projectId]/route.ts (PATCH renames, DELETE deletes); 401 for unauthenticated, 403 for non-owner mutations; uses Clerk auth() and Prisma; no UI wiring.
- Feature 07: Wire Editor Home — editor/page.tsx converted to RSC; fetches owned projects (by userId) and shared projects (by email via ProjectCollaborator) server-side using lib/projects.ts helpers; EditorHomeClient client shell holds sidebar toggle state; useProjectActions hook manages all dialog state + API calls (create with client-generated room ID slug-suffix, rename with refresh, delete with redirect-or-refresh); project-sidebar and project-dialogs updated to use real SidebarProject type and wire handleCreate/handleRename/handleDelete; POST /api/projects accepts optional client-provided id for room ID alignment; mock-projects.ts and use-project-dialogs.ts removed.
- Feature 08: Editor Workspace Shell — lib/project-access.ts with getCurrentIdentity() (Clerk userId + primary email) and getProjectWithAccess() (owner or collaborator check); components/editor/access-denied.tsx (centered, lock icon, message, back link); app/editor/[roomId]/page.tsx RSC with auth redirect, access check, parallel data fetch, AccessDenied on missing/unauthorized; components/editor/editor-workspace-client.tsx client shell with sidebar toggle + AI sidebar toggle state, workspace navbar (project name center, share + AI toggle + UserButton right), ProjectSidebar with activeProjectId highlighting, canvas placeholder, AI sidebar slide-over placeholder; ProjectSidebar updated with optional activeProjectId prop to highlight current room.
- Feature 09: Share Dialog — app/api/projects/[projectId]/collaborators/route.ts (GET list with Clerk enrichment — name + imageUrl from getUserList; POST invite owner-only; DELETE remove owner-only; all with auth + ownership enforcement); components/editor/share-dialog.tsx (owner: invite input + remove-per-row + copy link with Copied! feedback; collaborator: read-only list; avatar with initial fallback); getProjectWithAccess now returns isOwner; EditorWorkspaceClient passes isOwner to ShareDialog and wires Share button to open it.
- Feature 10: Liveblocks Setup — liveblocks.config.ts updated with Presence (cursor + isThinking) and UserMeta (name, avatar, color); lib/liveblocks.ts created with lazy-initialized cached Liveblocks node client (globalThis singleton) and getCursorColor() that deterministically maps userId to one of 8 palette colors; POST /api/liveblocks-auth requires Clerk auth, verifies project access via getProjectWithAccess, calls getOrCreateRoom, returns access token session with name/avatar/color userInfo; @liveblocks/node installed.
- Feature 11: Base Canvas — types/canvas.ts with CanvasNodeData (label, color, shape) and CANVAS_NODE_TYPE/CANVAS_EDGE_TYPE constants; components/editor/canvas-flow.tsx with useLiveblocksFlow (suspense, empty initial nodes/edges), ReactFlow (ConnectionMode.Loose, fitView), MiniMap, dot-pattern Background, and Cursors; components/editor/canvas-room.tsx with LiveblocksProvider (/api/liveblocks-auth), RoomProvider (initialPresence cursor null + isThinking false), CanvasErrorBoundary class component, and ClientSideSuspense loading fallback; EditorWorkspaceClient canvas placeholder replaced with CanvasRoom.
- Feature 12: Shape Panel — types/canvas.ts extended with NODE_SHAPES (6 shapes), NODE_COLORS (8 color pairs), DEFAULT_NODE_COLOR, NodeShape type, ShapeDragPayload interface; components/editor/canvas-node.tsx basic bordered-rectangle renderer (Handle at all 4 sides, dynamic fill/text color from NODE_COLORS, selected state border); components/editor/shape-panel.tsx floating pill toolbar (absolute bottom-center, 6 draggable icon buttons setting application/canvas-shape dataTransfer payload with shape + dimensions); canvas-flow.tsx refactored into ReactFlowProvider wrapper + CanvasFlowInner (useReactFlow for screenToFlowPosition, useRef node counter, onDragOver + onDrop handlers, CANVAS_NODE_TYPE nodeTypes registration, ShapePanel overlay); useLiveblocksFlow typed with CanvasFlowNode generic to allow NodeAddChange.
- Feature 13: Node Shape Rendering — canvas-node.tsx replaced with proper shape rendering: CSS shapes (rectangle uses rounded-xl, pill/circle use rounded-full) and SVG shapes (diamond polygon, hexagon polygon, cylinder with ellipses + rect) all using fill/stroke from node color pair; border uses --border-default at rest and --accent-primary when selected; ShapeBody exported for reuse; shape-panel.tsx adds drag ghost preview: native drag image suppressed, DragState tracks cursor position via onDrag, fixed-position ghost renders ShapeBody at cursor offset, cleared on dragEnd.
- Feature 14: Node Resizing and Label Editing — canvas-node.tsx adds NodeResizer (visible when selected, min 80×40, subtle cyan handles at 0.7 opacity and lines at 0.3 opacity, syncs via onNodesChange to Liveblocks); inline label editing via double-click opens a textarea overlay (flex-centered wrapper, auto-height, bg-transparent matching node text color); span hidden with opacity-0/pointer-events-none while editing; commit on blur or Escape via useReactFlow().updateNodeData(); all keyboard events stop propagation to prevent canvas drag/pan interference.
- Feature 15: Node Color Toolbar — canvas-node.tsx adds a floating pill toolbar that appears above selected nodes (8px gap, centered, bg-elevated with border-default, drop shadow); 8 ColorSwatch buttons (one per NODE_COLORS pair, 16×16 rounded-full); active swatch shows a 2px text-colored ring; hover shows a tight 4px glow in the swatch's text color at 30% opacity; clicking a swatch calls updateNodeData to sync the new fill color to Liveblocks; all toolbar interactions stop mousedown/pointerdown propagation to prevent node drag; outer div uses overflow:visible to allow toolbar to render above node bounds.
- Feature 16: Edge Behavior — CanvasEdgeData interface (label?: string) added to types/canvas.ts; components/editor/canvas-edge.tsx created with getSmoothStepPath for right-angle routing, invisible 20px-wide hit path for easier hover/click, BaseEdge renderer with stroke color transitioning from --border-subtle at rest to --text-secondary on hover/select, EdgeLabelRenderer positioning label at path midpoint (labelX/labelY), double-click to edit with auto-sizing input, pill badge for saved labels, faint "Label..." hint when active with no label, all label events stop propagation; canvas-node.tsx updated with nodeHovered state, onMouseEnter/Leave on outer div, all 4 handles (top/right/bottom/left, all type="source" with IDs) styled as 8px white dots with --border-subtle border, opacity 0→1 on node hover; canvas-flow.tsx adds edgeTypes mapping and defaultEdgeOptions (type: canvasEdge, markerEnd: ArrowClosed with currentColor).
- Feature 17: Canvas Ergonomics — hooks/useKeyboardShortcuts.ts created with window keydown listener, editable-target guard, and handlers for +/= (zoom in), - (zoom out), Cmd/Ctrl+Z (undo), Cmd/Ctrl+Shift+Z / Cmd/Ctrl+Y (redo); CanvasControls component added to canvas-flow.tsx at bottom-left with pill toolbar containing zoom-out/fit-view/zoom-in and undo/redo button groups separated by a divider, zoom uses 200ms animation, undo/redo wired to useHistory/useCanUndo/useCanRedo from @liveblocks/react with disabled buttons dimmed; MiniMap removed.
- Feature 18: Starter Templates — components/editor/starter-templates.ts defines CanvasTemplate interface and CANVAS_TEMPLATES array with three templates (Microservices, CI/CD Pipeline, Event-Driven System) built from shared canvas types and NODE_COLORS; components/editor/starter-templates-modal.tsx renders a Dialog with a scrollable 3-column card grid, each card showing an SVG preview (calculated viewBox, edge lines between node centers, pure-SVG shape rendering per shape type) plus name, description, and an Import button; canvas-flow.tsx accepts pendingTemplate/onTemplateApplied props — CanvasFlowInner uses a useEffect to clear all existing nodes/edges via remove changes then add template nodes/edges via add changes, then fitView after 80ms; canvas-room.tsx threads the props through; editor-workspace-client.tsx adds templatesOpen + pendingTemplate state, a LayoutTemplate navbar button to open the modal, and passes the props to CanvasRoom.
- Feature 19: Presence Avatars & Live Cursors — liveblocks.config.ts Presence renamed isThinking → thinking; canvas-room.tsx initialPresence updated; canvas-flow.tsx adds CollaboratorAvatar and PresenceAvatars components (absolute top-right of canvas, useOthers from @liveblocks/react/suspense, useAuth from @clerk/nextjs, filters out current Clerk user, overlapping stack of up to 5 avatars with +N overflow chip and ring-2 ring-base, divider shown only when collaborators exist, Clerk UserButton alongside); useUpdateMyPresence wired to onMouseMove/onMouseLeave on the canvas div to broadcast cursor position in flow coordinates; Cursors from @liveblocks/react-flow renders other participants' live cursors with colored pointer and name badge; editor home navbar left unchanged.
- Feature 20: AI Sidebar Shell — inline AI sidebar placeholder in editor-workspace-client.tsx extracted into components/editor/ai-sidebar.tsx; sidebar header with Bot icon, "AI Workspace" title (text-copy-primary), "Collaborate with Ghost AI" subtitle (text-copy-muted), and close button; two-tab layout using shadcn Tabs with AI Architect and Specs tabs, active tab styled with bg-ai/text-white; AI Architect tab has empty state with Bot icon + description + 3 starter prompt chips (bg-subtle, text-ai-text), scrollable message area with right-aligned user messages (bg-brand-dim, border-brand/50) and left-aligned assistant messages (bg-elevated, text-ai-text), auto-resizing textarea (min 72px, max 160px), Send button (bg-ai), Enter-to-send/Shift+Enter-newline; Specs tab has Generate Spec button (bg-ai) and demo spec card (bg-elevated, border-surface-border) with FileText icon, title, snippet, and disabled Download action; X import removed from editor-workspace-client.tsx.
- Feature 21: Canvas Autosave — @vercel/blob installed; canvasJsonPath field already existed on Project model (no migration needed); PUT /api/projects/[projectId]/canvas uploads canvas JSON to Vercel Blob (addRandomSuffix: false for deterministic URL) and stores URL on Prisma project record, owner-only; GET /api/projects/[projectId]/canvas reads blob URL from Prisma, fetches JSON from blob, returns canvas, accessible to owner and collaborators; hooks/use-autosave.ts debounces saves (2 s), tracks saving/saved/error status, skips first render to avoid save-on-load; canvas-flow.tsx CanvasFlowInner runs useAutosave, propagates status via onSaveStatusChange callback, loads saved canvas on mount only when Liveblocks room is empty (node/edge count captured in refs at mount); canvas-room.tsx threads projectId (= roomId) and onSaveStatusChange to CanvasFlow; editor-workspace-client.tsx manages saveStatus state via useCallback handler, shows Saving…/Saved/Error saving text in navbar center area.
- Feature 22: Design Agent API — TaskRun model added to prisma/models/task-run.prisma (runId unique, projectId, userId, createdAt; indexes on runId and userId+projectId compound); migration 20260626173409_add_task_run applied; trigger/design-agent.ts exports designAgent task (id: "design-agent", logs and echoes prompt+roomId payload, no AI logic); POST /api/ai/design validates prompt/roomId/projectId, checks owner, triggers designAgent via tasks.trigger, creates TaskRun record, returns runId; POST /api/ai/design/token validates runId, verifies TaskRun ownership by userId, issues Trigger.dev public token scoped to that run, returns token.
- Feature 23: Design Agent Logic — trigger/design-agent.ts fully implemented: uses Gemini 2.0 Flash via @ai-sdk/google with generateObject to produce structured canvas actions (add_node, move_node, resize_node, update_node_data, delete_node, add_edge, delete_edge); applies actions to Liveblocks room via liveblocks.mutateStorage creating LiveObject/LiveMap structures matching the @liveblocks/react-flow internal schema (flow.nodes/flow.edges); sets AI presence (userId "ai-ghost", thinking state, 5-min TTL) via liveblocks.setPresence with auto-expiry on completion; broadcasts AI_STATUS events (thinking/processing/complete/error) via liveblocks.broadcastEvent; validates node colors against palette; liveblocks.config.ts RoomEvent type added; canvas-flow.tsx AiStatusBanner component listens to AI_STATUS events via useEventListener and shows a floating status pill visible to all collaborators, auto-hiding after 4s on complete/error; ai-sidebar.tsx wired to POST /api/ai/design and /api/ai/design/token, uses useRealtimeRun from @trigger.dev/react-hooks via a RunTracker child component to track run status, shows typing indicator while running, adds summary as assistant message on completion; editor-workspace-client.tsx passes projectId/roomId to AiSidebar.
- Feature 24: AI Presence State — shared AI activity indicators visible to all room participants; LiveblocksProvider+RoomProvider lifted from canvas-room.tsx to editor-workspace-client.tsx so both canvas and AI sidebar share the same room context; ai-sidebar.tsx subscribes to AI_STATUS broadcast events (the "ai-status-feed") via useEventListener, validates messages through aiStatusFeedPayloadSchema from types/tasks.ts, shows an inline status banner below the header when active, disables chat input and shows a spinner on the send button when generation is active from any participant; CanvasCursor in canvas-flow.tsx reads thinking from other users' presence and passes a ReactNode label with a spinning indicator to the Cursor component when thinking is true; types/tasks.ts defines aiStatusFeedPayloadSchema with optional text field.
- Feature 25: Sidebar Chat Feed — real-time room chat added to the AI sidebar as a dedicated "Chat" tab (separate from "Architect" and "Specs" tabs); chatMessageSchema (id, sender, role, content, timestamp) added to types/tasks.ts; liveblocks.config.ts Storage type extended with aiChat: LiveList<ChatMessage>; RoomProvider in editor-workspace-client.tsx initialized with initialStorage { aiChat: new LiveList([]) }; Chat tab subscribes to aiChat via useStorage, validates each message with chatMessageSchema before rendering, shows sender name and formatted timestamp per message; useMutation pushes new messages to the LiveList; useSelf provides the sender name from Liveblocks UserMeta; input clears on successful send, shows red error text if send throws; chat tab auto-scrolls to bottom on new messages; ai-chat feed kept strictly separate from ai-status-feed (broadcast events).
- Feature 26: Design Agent Frontend — chatMessageSchema extended with optional source field ("architect" | "chat") to separate design messages from room chat; --accent-green (#62C073) added to globals.css as a Tailwind token; ai-sidebar.tsx Architect tab now pushes user design prompts and AI responses (source: "architect") to aiChat LiveList via useMutation for cross-session visibility, reads and filters by source === "architect" to display only design messages; Chat tab now tags messages with source: "chat" and filters to !source || source === "chat"; local messages state removed from Architect tab; status strip relocated to above the input area, shown only when a run is active (blocked); user chat bubbles use bg-accent-green/text-white, AI bubbles use bg-elevated/text-copy-muted; submit button uses bg-accent-green; errors pushed as assistant messages to aiChat; RunTracker still uses useRealtimeRun with token from /api/ai/design/token; canvas updates rely on Liveblocks with no manual sync.
- Feature 27: Spec Generation Flow — POST /api/ai/spec accepts roomId, chatHistory, nodes, edges; resolves project from roomId via getCurrentIdentity + getProjectWithAccess (no client-supplied projectId trusted); triggers generate-spec task; creates TaskRun record for ownership tracking; returns runId; POST /api/ai/spec/token verifies TaskRun ownership by userId, issues Trigger.dev public token scoped to that run with 1h expiry; trigger/generate-spec.ts uses schemaTask with full Zod validation, calls Gemini 2.0 Flash via @ai-sdk/google@3.0.0 (downgraded from 4.0.0 to match ai package's @ai-sdk/provider@3.x peer dep), builds canvas description from nodes/edges and filters architect chat messages for context, updates run metadata (status, progress) for realtime tracking, returns { spec: string } as plain Markdown.
- Feature 28: Spec Persistence & Download — ProjectSpec model added to prisma/models/project.prisma (id, projectId FK to Project with cascade delete, filePath, createdAt; index on projectId); migration 20260626234311_add_project_spec applied; trigger/generate-spec.ts updated to upload Markdown to Vercel Blob at specs/{projectId}/{specId}.md (access: private, allowOverwrite: false), create ProjectSpec record using randomUUID() as the ID, return { spec, specId } from task; GET /api/projects/[projectId]/specs/[specId]/download authenticates via getCurrentIdentity, checks project access via getProjectWithAccess, verifies spec belongs to the project, fetches blob content from ProjectSpec.filePath, returns as text/markdown attachment.
- Feature 29: Spec UI Integration — GET /api/projects/[projectId]/specs lists all specs for a project (id, createdAt, filename derived as spec-{id}.md), auth-gated via getCurrentIdentity + getProjectWithAccess; ai-sidebar.tsx Specs tab replaced with real list: fetches specs when tab activates (onValueChange), displays filename + formatted date per item with inline download button; clicking an item opens a shadcn Dialog modal that fetches content via the download endpoint (JS fetch, not browser nav), renders Markdown via marked.parse() with dangerouslySetInnerHTML, and provides Close + Download actions; download uses programmatic anchor with Content-Disposition attachment; TypeScript clean.

## In Progress

- None.

## Next Up
- Add the next planned feature unit here.

## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- Next.js 16 uses proxy.ts (renamed from middleware.ts) with export function proxy(). clerkMiddleware returns NextMiddleware which is compatible with NextProxy.
- Prisma v7 PrismaClient constructor requires either `adapter` (SqlDriverAdapterFactory) or `accelerateUrl` (string) — no zero-argument constructor. The lib/prisma.ts singleton branches on DATABASE_URL prefix to pick the right option.

## Session Notes

- Add context needed to resume work in the next session.
